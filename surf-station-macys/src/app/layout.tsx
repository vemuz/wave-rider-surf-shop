import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import ClientBody from "./ClientBody";
import { CartProvider } from "@/contexts/CartContext";
import Script from "next/script";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Scrole - World's Largest Surfboard Selection | Surf, Skate & Beach Gear",
  description: "Discover the world's largest selection of surfboards, wetsuits, skateboards, and beach gear at Scrole. From beginner to pro, we've got everything you need to make waves.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <head>
        <Script
          crossOrigin="anonymous"
          src="//unpkg.com/same-runtime/dist/index.global.js"
        />
      </head>
      <body suppressHydrationWarning className="antialiased">
        <CartProvider>
          <ClientBody>{children}</ClientBody>
        </CartProvider>
      </body>
    </html>
  );
}
